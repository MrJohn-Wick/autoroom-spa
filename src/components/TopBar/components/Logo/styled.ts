import styled from 'styled-components';

export const LogoStyled = styled.div`
  display: flex;
  flex-direction: row;
  align-items: center;
  
  span {
    font-size: 24px;
    font-weight: 700;
    margin-left: 10px;
  }

`;